# Prior Knowledge Baseline

用户已具备以下基础（2026-06-14 评估）：

- **Agent 概念**：理解 Agent = LLM + memory + planning + tool use，知道 Agent 是 Skills 和 MCP 的使用主体
- **Skills 结构**：能独立设计 skill，理解元信息 → 指令层 → 资源层的三层结构，会使用渐进式披露（references 按需加载）
- **Prompt Engineering**：理解如何写指令来控制 AI 的输出格式和行为边界
- **Context Engineering**：知道上下文过多/过少都会影响 AI 表现，但不清楚「什么样的上下文才算好」
- **Harness Engineering**：了解概念（Agent = Model + Harness），读过 6 条关键原则，但尚未在实战中应用
- **MCP**：了解是统一工具接入协议，知道元信息会占用上下文，但从未实际调用过 MCP 工具

**已构建的 Skills**：study-note（学习笔记创建）、feynman-test（费曼学习法测试），两者结构完整、有检查清单。

**核心痛点**：缺少评估 skill 好坏的框架，不确定自己的 skill 设计在哪个水平。
