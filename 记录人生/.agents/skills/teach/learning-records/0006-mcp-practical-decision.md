# MCP 实战：从零到决策框架

用户对 MCP 的了解停留在概念层（统一工具接入协议），从未实际调用过，平时用 CLI 工具替代。但用户当前的 session 中已有 2 个 MCP 工具在运行（ide server 的 executeCode 和 getDiagnostics），只是没有意识到。

第 6 课的目标：建立 CLI vs MCP 的决策框架，并通过用户的 ide MCP 工具作为实战案例，将 MCP 从概念转化为可实际使用的工具。
