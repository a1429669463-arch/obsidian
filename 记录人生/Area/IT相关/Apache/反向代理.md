---
date: 2026-05-20
---

在采用 [[Apache]] 作为前置 Web 服务器或 反向代理的架构中，浏览器通常不会直接访问后端应用，而是先访问 Apache，再由 Apache 将请求转发给后端。

```
用户浏览器
   ↓ HTTP/HTTPS
Apache
   ↓ HTTP / AJP
Tomcat、WildFly、Spring Boot 等后端
```

在向后端发送请求时，通常有两种协议

> [[HTTP]]
> 
   [[AJP]]