---
date: 2026-05-13
tags:
  - IT
  - git
---
`git`相当于一个内容寻址的版本数据库，通过`commit id`能够快速去找到保存的各个版本的快照

其中`git commit`相当于保存一个当前仓库的快照,[[HEAD指针]]时刻指向当前的位置

在本地,`git`主要分为工作区,暂存区,本地仓库

在工作区改文件,通过`git add`将文件加入暂存区,方便后续`commit`,`commit`后进入本地仓库,最后是`push`




